[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "m365-extract"
version = "0.2.1"
description = "Microsoft 365 data extraction to Obsidian-compatible markdown via Graph API"
readme = "README.md"
license = "MIT"
requires-python = ">=3.12"
authors = [
    { name = "Matthias Christenson" },
]
keywords = ["microsoft-365", "graph-api", "obsidian", "markdown", "sync", "extraction", "email", "calendar", "teams", "onedrive", "sharepoint"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Topic :: Communications :: Email",
    "Topic :: Office/Business",
]
dependencies = [
    "msal>=1.34,<2",
    "httpx>=0.28,<1",
    "pyyaml>=6.0,<7",
    "python-frontmatter>=1.1,<2",
    "markdownify>=0.14,<1",
    "structlog>=25.0,<26",
    "click>=8.1,<9",
    "python-dotenv>=1.0,<2",
]

[project.optional-dependencies]
web = [
    "fastapi>=0.115,<1",
    "uvicorn>=0.34,<1",
    "apscheduler>=3.10,<4",
    "cryptography>=44,<45",
]
azure = [
    "azure-storage-blob>=12.24,<13",
]
convert = [
    "obsidian-import[markitdown,docling]>=1.0.1",
]
all = [
    "m365-extract[web,azure,convert]",
]
dev = ["pytest>=8.0,<9", "hypothesis>=6.100,<7", "pytest-cov>=5.0,<6", "pytest-httpx>=0.35,<1"]

[project.urls]
Documentation = "https://neuralsignal.github.io/m365-extract/"
Repository = "https://github.com/neuralsignal/m365-extract"

[project.scripts]
m365-extract = "m365_extract.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["m365_extract"]

[tool.hatch.build.targets.sdist]
include = ["m365_extract/"]

[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM"]
ignore = ["E501", "SIM108"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "integration: tests that require real Graph API credentials",
    "azurite: tests that require Azurite emulator",
]
